import React from "react";
import NavAdmin from "../admin/Nav";

const Header = (props) => {
    return (
        <header id="header" className="header admin-header">
            <NavAdmin />
        </header>
    );
};

export default Header;
